pre-commit:
  parallel: true
  commands:
    lint-check:
      run: npx biome check .

commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}

pre-push:
  parallel: true
  commands:
    test:
      run: npm run lint || true
    build:
      run: npm run build
